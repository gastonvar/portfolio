/**
 * Inner column inside each section’s `max-w-7xl` shell so headings and cards
 * share the same max width across Projects, About, Education, and Contact.
 */
export const SECTION_INNER_CLASS = 'mx-auto w-full max-w-6xl';
